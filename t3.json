{
  "$schema": "https://t3.codes/schema/t3.json",
  "scripts": [
    {
      "name": "Setup Worktree",
      "command": "git fetch origin main && git merge --ff-only origin/main && pnpm install --frozen-lockfile",
      "icon": "configure",
      "runOnWorktreeCreate": true
    }
  ]
}
